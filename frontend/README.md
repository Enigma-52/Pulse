# frontend

Public-facing Pulse frontend app.

## Purpose

This app is for:
- Landing pages
- Documentation entry points
- Public marketing/product pages

This app is **not** the in-product dashboard UI. The product dashboard app is `future-web/`.

## Run

```bash
cd frontend
npm install
npm run dev
```

Default dev URL: `http://localhost:5174`
